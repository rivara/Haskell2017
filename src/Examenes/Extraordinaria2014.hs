
module Examenes.Exytraordinaria2014 where
--E2
-- Definiciones
--data Studient =Studient {fullname:: String, age::Integer ,qualifications::[Int]}
--data Tree a = Empty |Node (Tree a) a (Tree a) deriving Show

--insert :: (Ord a)=>Tree a -> a ->Tree a
--insert Empty x= left x
--insert (Node x l r) y= case compare y x of
--	Gt -> Node x l (insert x y)
--	 _ ->Node x (insert l y) r

	
-- Studient n a qs
-- falta definir left

--E3 GRAFOS no